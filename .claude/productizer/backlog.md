# Productizer — backlog

Everything wanted and not yet agreed. This is the queue **in front of** the
lifecycle: nothing here has been through intake, so nothing here is a
requirement. An item leaves this file by becoming an intent at Stage 1, and what
happens to it after that is decided by intake, not by whoever wrote it down.

Location
: `.claude/productizer/backlog.md`, beside the living spec, in the spec home repo.

Order is priority
: Top is next. The file's order **is** the ranking — there is no priority field,
because two orderings disagree the moment someone edits one and not the other.
Reordering is an ordinary edit and needs no ceremony: nothing here is agreed, so
nothing is being changed by moving it.

Ids
: Backlog items take `B<n>` and keep it. `B` never shares a counter with `R`
(requirements), `P` (principles) or `D` (rulings). An item that becomes an
intent keeps its `B` id in the intent so the trail from "someone wanted this" to
"the spec now says this" survives.

Next backlog id
: `B46`

## Status

| Status | Means | Who moves it |
|---|---|---|
| `todo` | wanted, ready to be picked up | anyone |
| `long-term` | wanted, deliberately not now | anyone, with a reason |
| `in-progress` | Stage 1 has started on it | whoever started |
| `blocked` | cannot proceed, and the blocker is named | whoever found the blocker |
| `done` | it went through intake and reached the spec, or was refused | intake |

**`done` does not mean built.** It means the item left this queue — merged as a
requirement, ruled a duplicate, or refused. The spec records which. A backlog
that reuses `done` to mean shipped starts competing with the spec for the
answer to "what does this system do", and loses.

**An item with a Jira key does not carry its own status.** See below.

## Items

Highest priority first. **Open items lead, and the order below *is* the
ranking** — there is no priority field, so moving a row is how a priority
changes. Closed items follow in id order; a finished item has no priority left
to carry.

| Id | What is wanted | Status | Jira | Raised | Notes |
|---|---|---|---|---|---|
| B3 | The solver misses semantic contradictions | `in-progress` | — | maintainer, 2026-08-28 | 0.70 recall on the 21 cases we wrote first, **0.12 on a harder 26-case corpus** — the figure tracked the corpus, not the checker. **Started 2026-09-02. One unsound inference removed; recall did NOT move.** `compare()` treated `GUARD_UNRELATED` as proof the guards could not both hold and returned CONSISTENT. That relation is purely lexical - it means the two guards share no vocabulary - while `GUARD_DISJOINT` is earned, by negation or by non-meeting ranges on one quantity. *While dunning is active* and *when the cart is abandoned* share no words and describe the same moment, so the checker was answering CONSISTENT about a pair it had not examined. It now carries the weight of UNKNOWN: an incompatibility found under it escalates to UNDECIDED rather than convicting or acquitting. **Measured, and the honest result is a null one:** corpus recall stays **0.12** and precision **0.67**, unchanged, because the four cases resting on that rule - P02, P04, P15, N07 - all still end at *no incompatibility inside the decidable fragment*. The guard rule was never their binding constraint; the RESPONSE analysis is, and cancel/terminate and expunge/archive are synonyms rather than antonyms, which `exclusive_responses` does not resolve. **The fix is still load-bearing and is now pinned by a test.** Selftest case `U2` - lexically disjoint guards, exclusive responses - reads CONSISTENT without the change and UNDECIDED with it, so a silent wrong pass became an escalation. Falsified by reverting the fix while keeping the case: `*U2 UNDECIDED CONSISTENT`. **Step 2, 2026-09-02 — the bound parser. Selftest moved, corpus did not.** Two defects, both mechanical. `weeks` was in neither `UNITS` nor `BOUND_RE`, so `6 weeks` fell through to the dimensionless `count` default - WORSE than failing to parse, because a bogus dimension can never conflict with a bound in days and the miss is silent. And `BOUND_RE` required `\d+`, so `six weeks` produced no bound at all. Both fixed: weeks added (exactly seven days, so it converts without a convention), spelled numbers one to twelve accepted, and a unit the regex matches but `UNITS` cannot convert is now DROPPED rather than given a false dimension. **Month and year were deliberately left out** - a month is 28 to 31 days and choosing one would let this file decide a contradiction by rounding. Measured: selftest recall **0.64 to 0.73** with precision holding at **1.00** and zero false positives, pinned by case `W1` and falsified by reverting the parser on the identical case set (`*W1 CONTRADICTION CONSISTENT`, recall back to 0.64). Corpus recall stays **0.12**. **And a correction to the ranking below, which was mine and was wrong.** P10 is not arithmetic: it sets *99.9 percent of each month* against *90 minutes of downtime per month*, which needs a percentage of a window converted into a duration, so it needs the window's length and inherits the month ambiguity. It is a semantic judgement wearing numbers. **P11's real blocker is now identified and it is not units:** guards are identical and both bounds parse, but the numeric branch is gated on `same_quantity`, and *remove all personal data* against *hold in a reversible grace period* share too little vocabulary to pass it. **Step 3, 2026-09-02 — the lexicon. Recall finally moved: 0.12 to 0.38, and precision went UP with it, 0.67 to 0.83.** Six disposition pairs the corpus opposes that the shipped lexicon did not carry - remove/retain, remove/preserve, expunge/move, decline/honour, suspend/keep, abandon/retry. They were not guessed: `solver-probe --break lexicon` already held them and had measured what adding them was worth, so the gap was quantified before it was closed. Caught goes 2 to 5, silent misses 14 to 8, escalations 0 to 3, and selftest recall 0.73 to 0.82 with precision still 1.00 and no false positives. **The ablation that proved it was then repaired, because closing the gap broke it.** With the pairs shipped, `--break lexicon` adds duplicates and reports a zero delta - which reads exactly like *the lexicon does not matter*, a control asserting over an empty set, the failure that has bitten this repository twice. It now computes what is genuinely absent and ANNOUNCES an empty run instead of executing one. `--break guards` still bites, so the other control is alive. **And a correction: transitive chains were never the gap.** The previous entry ranked them next because *a pairwise sweep cannot reach them*. `solver-probe` is pairwise BY CONSTRUCTION - it renders each intent as the EARS requirement it would become and pairs it with the requirement it collides with - so there is no transitive dimension in the measurement at all. Checked directly: P13 already reached `UNDECIDED: mutually exclusive responses: 'keep' against 'end', but guards share 14% of their terms`, and P12 returned CONSISTENT for want of a deny/serve pair and a *slow* against *does not answer within 200 ms* guard match. Both are pairwise-reachable and blocked by VOCABULARY, not by arity. **Step 4, 2026-09-03 - the nine read one at a time, as the entry above said to. Silent misses 8 to 6, and one planned fix abandoned on measurement.** `(continue, stop)` and `(deny, serve)` were absent while their near-neighbours were carried - `(start, stop)` and `(continue, abort)` were both there. Adding them moves P13 and P12 from CONSISTENT to UNDECIDED, falsified by comparing the two module versions on the same pairs. **Read the counts, not the recall.** True positives stay at 5 and the false positive stays at 1. Recall reads 0.38 to 0.45 ONLY because an escalated case leaves the must-halt denominator, 13 to 11 - a denominator artefact, not a catch. The honest figure is silent misses **8 to 6**, and silence is the failure `solver.md` opens by naming. **N09 was going to be fixed and the measurement said no.** The antonym branch fires on stems with no check that the two verbs act on the same object, while the status-code branch three lines below already gates on content overlap - so the plan was to apply the same gate. Measuring the overlap of every pair where the branch fires killed it: N09 sits at **0.09**, and P01 sits at **0.09** with P04, P09 and P15 at **0.00**. Any threshold excluding the false positive excludes four true positives. N09 differs by REFERENT - *another tenant's records* against *that tenant's own figures* - which no lexical overlap measure sees. It stays open, and a threshold is the wrong instrument for it. **The six that remain, characterised individually rather than by name:** P02 needs cancel/terminate as guard synonyms AND stop-charging against issue-a-charge; P03's second requirement is *a second or two*, which is unquantified and should be refused by `validate-spec` before it ever reaches the solver; P05 needs *resolve a principal* recognised as *identity lookup* under a negation; P07 and P08 are constitution-level - cross-tenant leakage and an in-place field rename - and P08 looks the most mechanical of the six, being one identifier differing inside an otherwise identical response; P10 needs a percentage of a window turned into a duration; P12 and P13 now escalate and would need guard synonymy to convict. **Step 5, 2026-09-03 - P03 and P08, and neither belonged where it was.** P03 was never the solver's to catch. `ears.md` rule 5 forbids unquantified terms and `validate-spec` enforces it, but only as ADJECTIVES - `quickly` was caught and `a second or two` was not, and neither can be asserted by a test. Worse, `roughly 500 ms` passed while reading as measured. New `EARS_VAGUE_QUANTITY` warning catches the hedge WHERE IT QUALIFIES A QUANTITY, so `about 20 ms` is flagged and *information about the user* is not - the digit is required rather than the word listed. Falsified on P03's own pair: 1 warning with the rule, 0 without. The repository's own spec is unchanged at 0 errors and the same 5 warnings. **P08 escalates rather than convicts, deliberately.** The tokeniser strips punctuation, so `amount_cents` arrived as *amount* plus *cents* and two different field names looked like overlapping prose. `identifier_drift` reads snake_case and dotted names whole and reports when two responses name the same artefact and a different field for it. It returns UNDECIDED, not CONTRADICTION, because renaming a field in place breaks every consumer while adding one breaks none, and from here the two sentences are identical - only a person knows which was meant. Falsified both ways: CONSISTENT without it, UNDECIDED with it, and identical responses still resolve as a duplicate rather than drifting. **Silent misses 6 to 5**, escalations 5 to 6, true positives still 5, false positive still 1, precision still 0.83, selftest untouched at 0.82 and 1.00. **Known gap in this step, closed the same day.** `EARS_VAGUE_QUANTITY` was falsified against a scratch fixture and had no committed one, so nothing in CI would have noticed the rule going quiet. `validate-spec.py --self-test` runs in the workflow and already carried a `broken-spec` fixture asserting `EARS_UNQUANTIFIED`, so the new rule joins it there rather than getting a harness of its own - one more requirement in the fixture, one more code in the expectation. Falsified by disabling the rule and keeping the fixture: `SELF-TEST FAIL: broken-spec: expected EARS_VAGUE_QUANTITY`, exit 3. A rule nothing would miss is not covered, it is only currently correct. **Step 6, 2026-09-03 - and the characterisation in step 4 was wrong about two of the five.** Measuring the guard relation and the response test separately for each remaining case: P05's guards are already OVERLAP because one requirement is unconditional, and P07's are already EQUAL. Neither needs guard synonymy at all - that was my reading of the case names again, and both are blocked purely on the RESPONSE side. P02 needs both halves, not one. **The shape P05 and P07 share is a closed scope against a carve-out** - *resolve a principal for EVERY request* against *serve WITHOUT an identity lookup*, and *report ONLY that tenant's records* against *the median ACROSS ALL customers*. No antonym, no bound, so every other branch reads them as unrelated prose. **The rule was measured for noise BEFORE it was written**, which is the step N09 taught: swept over every pair in the corpus under an EQUAL or OVERLAP guard, it fires on exactly the two must-halt cases and on ZERO must-not-halt ones. That clean separation is what N09's overlap measure could not produce, and is the whole reason one was built and the other refused. **It escalates rather than convicts.** A carve-out from a universal is sometimes the intended exception and sometimes the thing that breaks it, and from inside this file they are the same sentence - `Where a route is public` is a legitimate exception to `every request`. That is a ruling, not an arithmetic result. Falsified: CONSISTENT to UNDECIDED on both, with N04 still quiet. **Silent misses 5 to 3**, escalations 6 to 8, true positives still 5, false positive still 1, selftest untouched at 0.82 and 1.00. **Three left, and none is lexical:** P02 needs guard synonymy AND a stop-charging against issue-a-charge opposition; P10 needs *99.9 percent of a calendar month* against *90 minutes* - decidable in principle, since 0.1% of even a 31-day month is under 45 minutes so 90 exceeds it for every month length, but it needs available and unavailable understood as complements; N09 needs referent awareness, and the measurement in step 4 says a threshold will not give it. **Step 7, 2026-09-03 - the false positive is gone and P10 is caught. Corpus precision 0.83 to 1.00, true positives 5 to 6, silent misses 3 to 2.** **N09: step 4's conclusion was too broad and is corrected here.** That measurement ruled out a THRESHOLD - the false positive sat at 0.09 overlap with a true positive at 0.09 and three more at 0.00 - and I generalised it to *no lexical approach works*, which does not follow. The difference is a REFERENT and it is named in the text: *another tenant's records* against *that tenant's own figures*. Swept over the eleven pairs where the antonym branch fires, that pattern matches the false positive and NONE of the ten true positives. Opposed verbs acting on different parties are not opposed, so the branch now returns nothing there. **P10: the window did not need guessing after all.** The objection was that a calendar month is 28 to 31 days, so a percentage of it cannot be turned into a duration. True, and unnecessary - the allowance is computed at BOTH ends and reported only when the stated duration exceeds even the most generous. 99.9% of a 31-day month leaves 44.6 minutes; the other requirement allows 90; it conflicts for every month length. A conflict that needs a particular February is not reported at all. **Both are pinned in the selftest, which is the part CI actually runs** - `solver-probe.py` is not wired into any check, so a corpus-only result would have had no guard. Cases `X1` and `B1`, falsified by disabling both rules and keeping the cases: `*X1 CONSISTENT CONTRADICTION` and `*B1 CONTRADICTION CONSISTENT`. Adding them surfaced a duplicate label - the first draft reused `N5`, which already existed - caught by counting labels rather than by reading them. **One miss left: P02**, which needs guard synonymy AND a stop-charging against issue-a-charge opposition. **Step 8, 2026-09-03 - the open question answered, and the answer was that the worry was unfounded.** The sweep fired on N07, a must-not-halt case, and the question was whether that harmed it. Simulated by forcing the guards to match and reading the verdict: N07 stays CONSISTENT, because its two responses are IDENTICAL and there is no opposition to find. P13 becomes a CONTRADICTION. P02 stays CONSISTENT even with guards matched, so guard synonymy alone was never going to fix it. Built as a CLOSED SET of lifecycle words - cancel/terminate/close/end and subscription/plan/account/membership - rather than a similarity score, because a threshold over general vocabulary is what starts matching guards that merely sound alike, and this file already carries one measured refusal for exactly that. **Two bugs found while building it, both by measuring rather than reading.** The first version collapsed `stems()` output against groups written in the singular, so nothing matched and the corpus did not move. The second stripped `es` from `closes` and got `clos`, which is in no group; it now offers every plausible singular to the lookup and lets the group's own spelling match. Neither would have been visible without checking the canonical jaccard directly - the corpus counts moved by coincidence in between, 6 to 7 and back to 6. **P13 is now caught rather than escalated.** True positives 6 to 7, false positives still 0, silent misses still 2. Pinned by `Y1` and by `Y2`, an anti-cry-wolf control where the same synonyms sit over the SAME response and must not produce a conflict - falsified by disabling the rule: `*Y1 CONTRADICTION UNDECIDED`, with Y2 unaffected either way. **P02 is the last miss and needs BOTH halves.** Its guards canonicalise to 0.50 because customer/subscriber are not in a group, and even at 1.00 its responses - *stop charging them* against *issue a final charge* - are not opposed by any pair in the lexicon. That second half is a cessation-of-an-activity against a performance of it, which is a new rule shape and wants its own noise sweep before anything is written. **Step 9, 2026-09-03 - P02 caught, and the one miss left is one the solver SHOULD miss.** Both halves swept before either was written. The cessation rule fires on P02 and P13 and on ZERO must-not-halt cases. The actor synonyms - customer/subscriber/member - fire on P02 and on N07, and N07 was checked by SIMULATION rather than assumed safe: it stays CONSISTENT, because its two responses are identical and matching guards give the response test nothing to find. **The opposition in P02 is not between two verbs.** *stop charging them* against *issue a final charge* share no antonym - `stop` opposes `start`, and nothing opposes `issue` - because the opposition is between a verb and its own cessation. `ceased_activity` requires exactly ONE side to be a cessation; when both stop the same thing they agree, which is this corpus's own duplicate shape and would have been the plainest false positive available. Pinned by `Z1` and by `Z2`, that exact both-cease control; falsified with both rules off - `*Z1 CONTRADICTION CONSISTENT`, Z2 unaffected. **Corpus: true positives 7 to 8, silent misses 2 to 1, false positives still 0, precision 1.00. Selftest recall 0.85 to 0.93 at precision 1.00.** **The remaining miss is P03 and it is correct.** Its second requirement says *a second or two*, which carries no bound to compare; `EARS_VAGUE_QUANTITY` refuses it in `validate-spec` before it ever reaches the solver. A checker that convicted on it would be inventing a number nobody wrote. B3's original complaint - 0.12 recall on the harder corpus - is answered: every case in it is now caught, escalated, or correctly out of scope | **Stage 1, 2026-09-05: classified `duplicate`.** Checked against the WHOLE living spec - all 32 active requirements (R1-R6, R9-R13, R15, R17-R20, R22, R24-R38 as active at this commit) - and all 4 constitution principles, by running `contradiction-check.py --pair` on this intent against every active requirement sentence. **No contradiction with any active requirement**, so nothing halted and nothing was merged into the spec. The obligation already exists and is agreed: **R33** (stop), **R34** (ask which wins), **R24** (merge nothing) and **R12** (merge nothing while unruled). This intent adds no obligation - it reports that `contradiction-check.py` under-delivers on requirements the spec already carries, so it is a defect against agreed text rather than a spec change. Nothing to merge into the spec. Prior status was `in-progress`.
| B35 | Generate the check from the requirement, not only the mapping to it | `long-term` | — | market survey, 2026-09-02 | Stage 4 already answers *which check asserts R29*; it cannot answer *there is no check for R29, here is one*. Competing tools generate tests from a specification and this is the nearest adjacent capability Productizer does not have — the acceptance-criteria table is exactly the input a generator needs, and `checks.yaml` is exactly the output shape. The hard part is not generation, it is **P1**: a generated check that exits clean has proven nothing until somebody has seen it fail, so anything emitted here has to arrive with a deliberately broken fixture attached and be refused if the fixture passes. A generator that produces green checks nobody falsified would manufacture exactly the hollow coverage the suite exists to catch |
| B20 | Control-framework tags for ISO 27001 and 42001 | `long-term` | — | maintainer, 2026-08-29 | `checks.yaml` already triggers on requirement tags. Tagging a requirement `A.8.28` or `42001-6.1.2` fires that control's checks on exactly the changes that touch it. Scope is the evidence layer under an ISMS - not risk assessment, SoA, asset inventory or internal audit . **Widened 2026-09-02 by the market survey.** The evidence layer is the half of a compliance story a tool like this can honestly own, and it is now written down: `references/compliance-map.md` states control by control which artifact evidences which obligation and, at greater length, which obligations Productizer produces nothing for. Tagging is the mechanical half that remains. Two rules the map already fixes and this item inherits: the verb is *evidences*, never *satisfies* or *complies*, because certification is an auditor's word; and ISO/IEC 42001 is paywalled, so its control text has not been read and is cited at title level with the third-party source named |
| B18 | The measurement suite has never run against a real model | `todo` | — | maintainer, 2026-08-29 | `ab-harness.sh` arms were printf and sleep; `retrieval-budget.sh` measures a character proxy that has never been calibrated against a token count. Both are honest about it in their own output; neither has produced a figure about this product | **First real run, 2026-09-05.** The harness ran against `claude-sonnet-5` on task P02, 5 rounds per arm, arms differing by exactly `--plugin-dir` and nothing else. Cost **$0.9598** over 10 priced calls. Result: the plugin arm cost **+112%** ($0.1378 against $0.0651 median) and took **+44%** longer (37706ms against 26190ms), fired the `spec` skill in 4 of 5 runs, and returned **the same verdict as bare in 10 of 10 runs**. Read with its limits: P02 is a case bare already passes 5/5, so this measures OVERHEAD ON A CASE THAT DID NOT NEED HELP, not whether the harness catches what bare misses; and 4 LLM graders worth 12 of 25 weight points were not run, so a benefit could live entirely there unseen. The status vocabulary held under its designed case: two 401 runs returned raw JSON containing `"total_cost_usd": 0` with `is_error: true`, and the harness recorded `incomplete`/`unavailable` rather than $0.0000. Still open: the second half (`retrieval-budget.sh` calibration), the other 25 cases, a task bare FAILS, and a significance test - n=5 is a difference of medians.
| B17 | Try two-factor contradiction detection | `long-term` | — | maintainer, 2026-08-29 | Scope overlap and directive polarity as independent gates rather than one similarity score, which conflates "same subject" with "opposite requirement". Trial against the 26-case corpus where the symbolic check scores 0.12. Do not take A comparable tool's lexicon: 12 hardcoded antonym pairs, reports without blocking |
| B36 | Agents other than Claude Code | `long-term` | — | market survey, 2026-09-02 | Claude Code only, against tools that install into thirty or more agents. The gap is real and the fix is not a port: enforcement here is PreToolUse hooks, and the halt, the publish gate and the deploy gate are all that mechanism. An agent with no hook equivalent can be handed the same instructions but not the same **stop**, which is the entire product. So the honest split is two tiers, named as such rather than blurred — agents that can be gated, and agents that can only be told — and a survey that establishes which is which before any code is written. Shipping a weaker gate under the same name would be worse than not shipping it |
| B4 | `jira.status_map` has never met a real Jira | `todo` | — | maintainer, 2026-08-28 | eleven states mapped, zero tested |
| B37 | A contract for third-party checks | `long-term` | — | market survey, 2026-09-02 | The largest spec-driven framework carries 157 community extensions; Productizer has no extension point at all. This is a distribution gap, not an engineering one, and it cannot be closed by building — but the enabling piece is small and is a prerequisite either way: `checks.yaml` already declares a command, an exit-code contract and a declared scope, so a third-party check is mostly a documented interface plus **P4**. That principle is the whole difficulty. A repository being examined never chooses what runs, so an extension mechanism that lets a cloned repo introduce a command is the exact hole `allow_repo_local_tools` exists to keep shut. Design the trust boundary first; the loader is the easy half |
| B2 | The gates cannot see inside a script | `long-term` | — | security review, 2026-08-28 | `./release.sh` is judged as its own name; no command-text gate can fix this |
| B22 | Scripts are untested off macOS | `long-term` | — | maintainer, 2026-08-29 | Every script was developed and run on macOS with bash 5.3 and BSD userland. GNU coreutils, GNU awk and bash 3.2 are argued, not measured |
| B5 | Release assets for offline install | `long-term` | — | maintainer, 2026-08-28 | only worth it if an install path without git is wanted |
| B38 | Enterprise identity — SSO, SCIM, RBAC | `long-term` | — | market survey, 2026-09-02 | Recorded to stop it being asked again, and the answer is **probably never, on architecture rather than effort**. There is no server, no account and no telemetry: the spec is a file in a repository the user already has permission to read, so there is nothing to authenticate to and no privilege for a role to withhold. Access control is the git host's, and it is already stronger than anything this tool could add. The market argument points the same way — four vendors shipped this in twelve months, so it will not differentiate anyone by 2027. **The half that is genuinely wanted is the audit trail, and that half already exists**: classification records with provenance, `D`-numbered rulings, `checks-result.json` with what each check examined, and waivers that keep the failure and name an authority. If this item is ever picked up it should be picked up as *evidence*, which is `B20`, not as identity |
| B39 | An editor or IDE surface | `long-term` | — | market survey, 2026-09-02 | Competing tools ship one; this is a terminal plugin and a generated read-only dashboard. Listed for completeness and ranked last deliberately. It is the largest build on this list, it competes with vendors who employ teams, and **P3** constrains the shape before it starts: a view never becomes an input, so an editor that let somebody type a requirement into a rendered panel would be the one thing the constitution forbids. What would be defensible is narrower and much cheaper — surfacing the halt, the pending ruling and the failing check where the work is already happening — and that should be scoped as its own item rather than as an IDE |
| B40 | Both of `solver-probe.py`'s advertised ablations are wrong | `in-progress` | — | doc pass, 2026-09-05 | The probe ships two interventions to prove it is live, and its own docstring names them: `--break lexicon` is "the intervention that must move recall", `--break guards` is "the intervention that must not". Measured 2026-09-05 by three direct runs at 4b4eb51, **both are backwards**. `--break lexicon` is a no-op and says so itself — it prints `--break lexicon adds nothing: every pair it holds is already in the shipped lexicon` and returns a matrix identical to the un-ablated one: true positives 8, false negatives 1, false positives 0, true negatives 10, undecided 7, recall 0.89. `--break guards` moves true positives 8 to 12, undecided 7 to 3 and recall 0.89 to 0.92. So the control moves and the intervention does not, and neither ablation currently demonstrates that the confusion matrix is live. This matters more since 2026-09-05, because `.github/workflows/checks.yml` now gates those same four counts against a dated baseline: the gate's evidence that it is measuring anything rests on ablations that are inverted. Fix is either to re-point `MISSING_PAIRS` at dispositions the corpus opposes and the lexicon still lacks, or to withdraw both claims from the docstring rather than leave a self-check that reads as passed. | **Stage 1, 2026-09-05: classified `duplicate`.** Checked against the WHOLE living spec - all 32 active requirements (R1-R6, R9-R13, R15, R17-R20, R22, R24-R38 as active at this commit) - and all 4 constitution principles, by running `contradiction-check.py --pair` on this intent against every active requirement sentence. **No contradiction with any active requirement**, so nothing halted and nothing was merged into the spec. Already forbidden by **P1** - a value that was not measured is never recorded as a measurement. The probe's docstring advertises two ablations as demonstrations that its confusion matrix is live; measurement shows `--break lexicon` moves nothing and `--break guards` moves what it says it will not. Claiming a demonstration that was not performed is the P1 case exactly, so no new requirement is needed to forbid it. Prior status was `todo`.
| B41 | `spec-doctor.sh` section 5 parses a sibling's summary line, and a reword silently disarms it | `in-progress` | — | doc pass, 2026-09-05 | Section 5 runs `pending-rulings.sh --format count` and picks the number out with `awk '/^[0-9]+$/'`. Falsified 2026-09-05 by substituting a stub that prints `pending rulings: 3` and exits 0: the doctor printed that line, then printed its *never raised one* explanation, and did **not** count a finding. Three rulings waiting on a human, no finding, no red. The brief that raised this named sections 4 and 5 together; section 4 is not the same defect — its grep only decides what is DISPLAYED, and its finding branch keys off `check-acceptance-rows.sh`'s exit code, so a reword there loses detail but still goes red. The fix is section 5 only: an unparseable count must be `unmeasured`, which the doctor already ranks above findings, rather than silence. | **Stage 1, 2026-09-05: classified `duplicate`.** Checked against the WHOLE living spec - all 32 active requirements (R1-R6, R9-R13, R15, R17-R20, R22, R24-R38 as active at this commit) - and all 4 constitution principles, by running `contradiction-check.py --pair` on this intent against every active requirement sentence. **No contradiction with any active requirement**, so nothing halted and nothing was merged into the spec. Already covered by **R26** - if a value could not be measured, the lifecycle shall not record it as zero. Section 5 prints its *never raised one* sentence when it cannot parse a sibling's count, which records an unparseable value as an absence. **R25** carries the positive half. Solver returned **UNDECIDED against R19** rather than a verdict: R19 governs an unreachable spec home and this governs an unparseable sibling count, the same shape on a different subject. Escalated, not convicted - recorded rather than resolved. Prior status was `todo`.
| B42 | `spec-doctor.sh` has no `--selftest`, so its finding branches are unproven in CI | `in-progress` | [#8](https://github.com/gitayg/productizer/issues/8) | doc pass, 2026-09-05 | `validate-spec.py` and `contradiction-check.py` both carry one and `.github/workflows/checks.yml` runs both. The doctor carries none — `grep -n 'selftest\|self-test'` over the file returns nothing — so nothing re-proves that a finding still exits 1, that a could-not-run section still exits 2, or that could-not-measure still outranks findings, as the five sibling tools it wraps change shape underneath it. B41 was found by hand-substituting a stub; a self-test over a constructed tree per branch would have found it, and would keep finding the next one. | **Stage 1, 2026-09-05: classified `extend`.** Checked against the WHOLE living spec - all 32 active requirements (R1-R6, R9-R13, R15, R17-R20, R22, R24-R38 as active at this commit) - and all 4 constitution principles, by running `contradiction-check.py --pair` on this intent against every active requirement sentence. **No contradiction with any active requirement**, so nothing halted and nothing was merged into the spec. No active requirement obliges a check tool to carry a self-test. **R5** requires a check to declare what it must have examined, and **R15** blocks a pass that examined less than declared - neither requires a tool to prove its own finding branches still fire. This is a NEW obligation and the only one of these seven that is, so it needs a requirement drafted and agreed before it is built. Prior status was `todo`.
| B43 | `check-suspect-links.sh` cannot see behind the base ref | `in-progress` | [#9](https://github.com/gitayg/productizer/issues/9) | doc pass, 2026-09-05 | `--base` defaults to HEAD, so the check compares the work tree against the last commit and never further. An **active** requirement whose sentence was rewritten in place several commits ago therefore reads as agreed text on every subsequent run, and every acceptance row, coverage claim and downstream citation of it keeps resolving to wording nobody re-read. Nothing else closes that: `check-superseded-text.sh` does read history, but only for **superseded** ids, and by its own header the two must not be collapsed — a supersede leaves a forward pointer, an in-place rewrite leaves nothing. The gap is an in-place rewrite of an active requirement older than whatever base the run is given. | **Stage 1, 2026-09-05: classified `extend`.** Checked against the WHOLE living spec - all 32 active requirements (R1-R6, R9-R13, R15, R17-R20, R22, R24-R38 as active at this commit) - and all 4 constitution principles, by running `contradiction-check.py --pair` on this intent against every active requirement sentence. **No contradiction with any active requirement**, so nothing halted and nothing was merged into the spec. No active requirement obliges the lifecycle to flag citations of a requirement rewritten before the base ref. **R3** keeps superseded text and `check-superseded-text.sh` reads history, but only for superseded ids; an in-place rewrite of an ACTIVE requirement leaves no pointer and no requirement covers it. A new obligation, narrower than B42's. Prior status was `todo`.
| B44 | Splitting the spec would make the now-ERROR `COUNT_MISMATCH` permanently red | `in-progress` | — | doc pass, 2026-09-05 | The spec header already anticipates the split: ids "stay unique across the whole repo even if this spec is later split into several files". `COUNT_MISMATCH` was promoted from WARN to ERROR, and it compares one header's declared totals against the requirements counted **in that one file**. Under a split there is no correct value to write in either file's header — a per-file total contradicts the product's total, and the product's total contradicts every per-file count — so the check would be red with no green state reachable, and the first thing anyone would do is switch it off. The severity has to be revisited in the same change that splits the spec, not after it, or the split ships with a gate nobody can satisfy. | **Stage 1, 2026-09-05: classified `refine`.** Checked against the WHOLE living spec - all 32 active requirements (R1-R6, R9-R13, R15, R17-R20, R22, R24-R38 as active at this commit) - and all 4 constitution principles, by running `contradiction-check.py --pair` on this intent against every active requirement sentence. **No contradiction with any active requirement**, so nothing halted and nothing was merged into the spec. Refines the reach of an existing obligation rather than adding one. **R1** holds that there is exactly one living spec per product, and the spec header already anticipates being split across files. `COUNT_MISMATCH` was promoted to ERROR in v4.44.0 against a single-file assumption; under a split there is no correct value to write in either header. The change is to the severity's condition, not to what the lifecycle owes. Prior status was `todo`.
| B45 | `run-checks.sh` accepts a change set that is not a list of paths | `in-progress` | — | maintainer, 2026-09-05 | Measured 2026-09-05: a `--changed` file holding `#!/usr/bin/env bash`, `# a comment, not a path` and `this is not a file at all` was accepted without a word. The runner recorded all three verbatim in `change.files`, computed coverage against them and wrote a result file that reads as a legitimate run. This is not hypothetical — `.claude/productizer/checks-result.json` in this tree holds **2611 entries of which 9 look like paths**; the rest are the source lines of `build-view.sh`, put there when a run was handed a script's contents instead of its change set. The damage travels: `pr-spec-comment.sh` reads that field and renders `Read from the recorded run over 2611 changed path(s)` into a comment a human is meant to paste onto a pull request, so a reviewer is shown a fabricated scope with no signal that it is wrong. The runner already knows how to ask whether a path exists — the deleted-file branch does exactly that — so the fix is to apply the same question to the whole change set on entry and refuse, not to teach the renderer to hide it. A change set nobody validated is not a measured scope, and every coverage number computed from it inherits that. | **Stage 1, 2026-09-05: classified `duplicate`.** Checked against the WHOLE living spec - all 32 active requirements (R1-R6, R9-R13, R15, R17-R20, R22, R24-R38 as active at this commit) - and all 4 constitution principles, by running `contradiction-check.py --pair` on this intent against every active requirement sentence. **No contradiction with any active requirement**, so nothing halted and nothing was merged into the spec. Already covered by **R15** - a check that exits zero having examined less than it declared is blocked - together with **R25** and **R26** on unmeasured values. A change set of 2611 entries of which none exist is examining nothing while reporting a scope, which is the hollow case R15 names. Measured downstream: with the corrupt record restored, `view-read-only` A2 goes from upheld 1/1 to 0/1 and back, three times, so the defect also breaks **R4**'s read-only guarantee for the generated view. Prior status was `todo`.
| B1 | Take one intent through all nine stages on this repo, through a PR | `done` | [#2](https://github.com/gitayg/productizer/issues/2) | maintainer, 2026-08-28 | Stage 8 has no deltas or PRs to draft from until this happens. **Started 2026-08-30** with B26 as the intent, issue [#2](https://github.com/gitayg/productizer/issues/2). Chosen because it is real, security-relevant, and forces the constitution check to do actual work: P4 is under-enforced by the very requirement listed as enforcing it **2026-09-01: the loop closed.** Three intents went from issue to classification to spec delta to PR to merge - #2/R29, #1/R30+R31, #3/R7->R32. The stack was rebased onto current main, every conflict regenerated rather than resolved by hand, and the suite run at each step. Merged locally; the push waits for the window. **Closed 2026-09-01.** Three intents went issue -> classification -> spec delta -> PR -> merge: #2 became R29, #1 became R30 and R31, #3 turned R7 into R32. Rebased onto current main, every conflict regenerated rather than resolved by hand, suite run at each step, and all three merged and pushed. |
| B6 | Scaffolded specs still carry placeholder rows | `done` | — | maintainer, 2026-08-29 | `\| R1 \| <area> \|` index rows and `P4`/`P5` amendment rows sit outside the example fences `scaffold.sh` strips. The parsers ignore them, so this is a documentation wart, not a seeded requirement. **Closed 2026-08-30.** The note's premise was half wrong and the bug was real anyway: the rows were already inside the example fences, but an HTML comment terminates a markdown table, so the rows fell out of it and `<area>` - a real HTML void element - was swallowed by the renderer entirely. Sample rows now sit in a text fence inside the example fence, so one stripping mechanism still does the work and the shape still renders. Falsified: the pre-fix templates leak 4 `<area>` rows and 2 `P4`/`P5` rows into a fresh scaffold, the fixed ones leak none. `scaffold.sh --version` also exited 2 against its own documented contract; fixed |
| B7 | Two scripts have no option contract | `done` | — | usage-audit, 2026-08-29 | `detect-context.sh` accepts any flag and exits 0; `scaffold.sh --help` exits 2. `import-survey.sh` had the same defect and is fixed. **Closed 2026-08-30.** `detect-context.sh`, `scaffold.sh` and `import-survey.sh` all answer `--help` 0, `--version` 0 and an unknown flag 2. Verified by running each |
| B8 | Make suppressed stderr a declared check | `done` | — | maintainer, 2026-08-29 | `2>/dev/null` hid three real defects in one session. A check over our own scripts would have caught each. **Closed 2026-08-30.** `check-stderr.sh` is declared as the `stderr-suppression` check and runs on every change. It is `advise`, not `block`, because it currently reports 66 pre-existing findings across 8 of 28 scripts - blocking on debt nobody has triaged would only teach people to bypass the gate |
| B9 | Split the requirements carrying two `shall` clauses | `done` | — | validate-spec, 2026-08-29 | Done 2026-08-29. R14/R16/R21 superseded with text retained, replaced by R23-R28. The split alone could not clear `--strict`: the EARS rules were being applied to superseded requirements, whose text must be retained verbatim, so the warning was unclearable. Fixed in the validator; `--strict` now exits 0 |
| B10 | The eval corpus has never been graded | `done` | — | maintainer, 2026-08-29 | Graded 2026-08-29: **16/16 recall, 0.94 precision** end to end, against **13/16** for a no-plugin baseline. The three it uniquely catches are domain entailment, an unquantified adjective against a bound, and vocabulary drift. 52 runs, $14.79. n is small and the corpus is self-authored; the harness is early-access |
| B11 | The agent halts on a contradiction and does not ask for the ruling | `done` | — | eval, 2026-08-29 | `06-ruling-requested` failed on P01, P11 and P12 in the graded run. The halt is correct and nobody is prompted for the decision, so the work stops and stays stopped. **Closed 2026-08-30 as a duplicate of R23**, which already requires the lifecycle to stop AND ask. Nothing needed agreeing, so no id was allocated: the want was agreed already and only the verifier was missing. Built: `request-ruling.sh` drafts the ruling and writes the concern row at the moment intake classifies contradict, so the ask cannot be the step someone forgets; `pending-rulings.sh` reports what is waiting, distinguishing never-raised from unreadable from a true zero; `check-ruling-requested.sh` is declared as the `ruling-requested` check and observes the ask rather than the stop. R23's acceptance row now names both halves separately |
| B12 | A committed learnings store, subordinate to the spec | `done` | — | maintainer, 2026-08-29 | Not everything learned is a requirement. "The build breaks unless you run X first" is nobody's obligation, and forcing it into `spec.md` corrupts the one thing that file is for - so there is a storage gap, not only a retrieval one. Put it in the repo (`.claude/productizer/learnings.jsonl`), committed and append-only, one entry per line with provenance, a status and optional requirement ids. **It sits below the spec and cannot outrank it**: a learning informs, it never obligates. A learning that contradicts an active requirement is a finding, not a fact - routed through intake like any other intent, the same road Stage 9 uses for code-versus-spec drift. One that turns out to be a real obligation graduates to an `R` id and stops being a learning. Two rules, both about not trusting yourself: **unverified until independently corroborated** - the same dispatch can never confirm its own learning, or an agent graduates whatever it just wrote - and **dormancy stops serving, never deletes**, with a missing timestamp never read as infinitely old, since that hides the entries with the least provenance to recover them by. Derive the corroboration threshold from a corpus audit rather than choosing it, and record the rejected alternatives: a gate that passes everything is the same as no gate. Productizer can do one thing a free-text store cannot - a learning may cite `R14`, and because ids are permanent, "this learning is about a requirement that has since been superseded" is mechanically detectable by the check `drift-reverse.sh` already runs over code. **Closed 2026-08-30.** `learnings.sh` with ids `L<n>`, stored as a directory of files beside the rulings and following the same header contract - deliberately not one append-only file, because a single file has no directory to be absent, so "nobody ever recorded one" and "there are none now" collapse into the same answer. Proven: the three store states give three outcomes (never-recorded 5, empty 0, unreadable 3, never a fabricated zero); a learning citing a requirement the spec later supersedes goes red; and corroboration by the source that made the observation is refused, which is the mechanism that stops the store agreeing with itself. NOT closed: nothing checks whether a learning is TRUE, contradiction with an active requirement needs a reader, and nothing in CI runs `learnings.sh check` yet |
| B13 | A check has no rendering after a human overrides it | `done` | [#7](https://github.com/gitayg/productizer/issues/7) | maintainer, 2026-08-29 | A ruling is recorded; what the *check* shows afterwards was never decided. A comparable tool renders `FAIL - WAIVED BY <authority>`: it keeps the failure and adds a named authority rather than flipping green. Worth knowing that theirs is emergent convention rather than written policy, so it is a pattern to weigh, not a standard to copy. **Stage 1, 2026-08-31 - classified `extend`.** Checked against the whole living spec and the constitution. **P1** (a value that was not measured is never recorded as a measurement) - not contradicted, because an overridden check WAS measured; but it constrains the answer hard, since its own words are *none of them produce a pass*, and a failure rendered green because somebody said so is a judgment wearing a measurement's clothes. **P2** (nothing reaches an audience without a person deciding) - reinforcing: a waiver IS that person, and naming them is the point. **R13**, **R15**, **R25**, **R26** - same family, all about states a check reaches on its own; none covers what it renders after a human overrides it. **R17** - about commands a person approves, not check output. **R5** untouched. Not `duplicate`: nothing active covers post-ruling rendering. Not `refine`: it adds an obligation rather than narrowing one. **No contradiction; nothing merged.** For intake: the rendering and the FORMAT that records the waiver are two obligations - a ruling file records the decision and no file format records the waiver, which is why this item has been blocked by design rather than by effort. Intent [#7](https://github.com/gitayg/productizer/issues/7), labelled `sdlc:intent` **Closed 2026-09-02, merged as R37 and R38.** Intake's split held: the rendering and the FORMAT are two obligations, so two ids. **P1 decided the design** - an overridden check WAS measured, so a waiver is permitted, but a failure rendered green because somebody said so is a judgment wearing a measurement's clothes. The status stays `fail`, the findings stay, the coverage stays; only the BLOCKING and the RENDERING move. Measured: waived exits 0 with `status: fail` recorded and `waived: 1`; the same check unwaived exits 3. **P4 decided the default** - waivers live in the repository, so a clone that could waive its own gates would arrive disarmed. `policy.waivers` is absent by default and the directory is never opened without it, the same shape as `allow_repo_local_tools`; the same waiver file on disk, one config line apart, is honoured or unread. A waiver also expires, selects nothing, and its authority is a label rather than a credential. **And a waiver does not waive the spec**: the check is still `fail`, so its coverage claims stay voided and the requirement returns to `Missing` - a person may accept a failing check, not un-assert a requirement |
| B14 | Nothing asserts one living spec per product (R1) | `done` | — | interrogation, 2026-08-29 | `product.spec_home` is declared in config.json and never read back. No declared check, no script that refuses on an ambiguous or unreachable home, none of validate-spec's 53 codes. R1 is the requirement most exposed to what it exists to prevent: two allocators both handing out R42. **Closed 2026-08-30.** `check-spec-home.sh` is declared as the `spec-home` check. It also had to be fixed on the way: it printed an absolute path into the runner's `output_tail`, which is committed, reproducing the v4.2.0 leak. Now repo-relative |
| B15 | 11 active requirements have no acceptance row | `done` | — | maintainer, 2026-08-29 | R1, R3, R4, R6, R7, R8, R9, R10, R12, R13, R19. The table answers "do the tests assert this" as a fact, so a row must come from an answer, never from reading the code and guessing. The dashboard's copy-prompt now interrogates rather than fills. **Closed 2026-08-30.** Every active requirement now carries an acceptance row - 25 rows, and the only three without one are R14, R16 and R21, which are superseded. The rows record what WILL count as evidence and say plainly that it is not built yet, because a decision about evidence is not a measurement. Two rows are confessions rather than plans: R7 is contested and too broad to satisfy as written, and R9 was violated twice the same day it was examined. Building the verifiers is B27 |
| B16 | The runner JSON format ships with no executor | `done` | — | maintainer, 2026-08-29 | `templates/runners/*.json` defines id, agent, timeout, sandbox scope, an anchored rubric and an output contract. Nothing in the repo runs one. It is a format, not a gate, and the site says so. **Closed 2026-08-30.** `run-runner.sh` validates a runner definition and executes it. Refuses on argv that names a shell, an interpreter with an inline program, `awk` in any position, a repo-local script, or any of those nested at any depth in the JSON - **every element of argv, not just the first**. Each refusal was falsified by removing the rule and watching the payload run. Note it is now STRICTER than `run-checks.sh`, which still validates `argv[0]` only and is a live finding. Not closed: nothing declares `run-runner.sh` as a check, and `sandbox.network`/`repo_token` are validated but not enforced |
| B19 | Graduated hook and CI-gate rungs are stubs that assert nothing | `done` | — | graduate, 2026-08-29 | The escalation ladder picks the right rung and writes a file that checks nothing. The lexicon knows a cluster is mechanically checkable; it cannot write the check. **Closed 2026-08-30.** The stub was literal: the emitted body was `set -euo pipefail` then `exit 0`, with a comment asking someone to replace it. Both asserting rungs now assert, and they assert DIFFERENT properties - the hook judges ONE PROPOSED ACTION from the tool payload before it runs, reaching text that never becomes a file; the gate judges EVERY TRIGGERING FILE already on disk and carries a coverage denominator, which is the one verdict a hook cannot give. Verified by generating a rung and feeding it payloads: compliant 0, violating 2, non-JSON 2, empty 2. A cluster is now checkable only if a rule is written for it, so the seven topics with no rule cap at skill and say why instead of emitting a stub that passes |
| B21 | Nothing lints markdown template frontmatter | `done` | — | delegation review, 2026-08-29 | Declared checks cover all files (hygiene), `**/*.sh` and the solver corpus. The agent templates carry frontmatter that decides tool access, and no check reads it. **Closed 2026-08-30.** Declared as the `template-frontmatter` check. The script existed but was never declared and had no `--version`, so the runner could not use it - it was written, not wired. Also printed absolute paths into the committed result file; now repo-relative |
| B23 | The dashboard could hand over its evidence as a file | `done` | [#1](https://github.com/gitayg/productizer/issues/1) | maintainer, 2026-08-29 | A published artifact may declare `downloads`, which is still output. It must not declare `artifact` - a page that saves new versions of itself becomes a second source of truth that can disagree with the repo, and the provenance line stops being true. **Stage 1, 2026-08-29 - classified `extend`.** Checked against the whole living spec and the constitution: P3 (a view never becomes an input) - compatible, `downloads` hands a file out and reads nothing back, and this item already refuses `artifact` for P3's own reason; P2 (nothing reaches an audience without a person deciding) - reinforcing, the viewer confirms each save; R4 (views read-only with respect to the spec) - compatible, the write lands on the viewer's disk, never on the spec; R11 (every figure read from a file in the repository) - constrains the design, the handed-over file must be composed from repository files rather than built in the browser. R1, R2, R3 and R5 untouched. Not `duplicate`: no active requirement covers emitting evidence. Not `refine`: it adds an obligation rather than narrowing one. **No contradiction; nothing merged.** For intake: the permission and the refusal are two obligations, so they need two ids - the same defect that forced R14, R16 and R21 to be split. Intent issue [#1](https://github.com/gitayg/productizer/issues/1), labelled `sdlc:intent` **Merged 2026-09-01 as R30 and R31.** The permission and the refusal became two ids, as intake required. R30 is Partial and R31 is Partial - `view-read-only` asserts what the page ASKS for, by source pattern, not what the publish step would refuse. **Closed 2026-09-01, merged as R30 and R31.** The permission and the refusal became two ids, as intake required. |
| B24 | Distinguish a bug from a feature | `done` | — | maintainer, 2026-08-29 | The two take different routes and the file cannot tell them apart. A feature is a want the spec does not yet cover, so it needs intake, a classification and a new id. A bug is the system failing a requirement that is already agreed — the spec is right and the code is wrong, so intake has nothing to decide and no id should be allocated. Today both are `todo` and whoever picks one up re-derives which it is. Open question intake must settle: a `Kind` column repeats the mistake the priority field was left out to avoid — a second field that disagrees with the first the moment someone edits one and not the other — so the alternative worth weighing is deriving it, since a bug is exactly an item that names the requirement it violates. **Closed 2026-08-31 by decision: use the tracker's own labels, add no field.** `bug` and `enhancement` already exist on the repo and mean exactly this. A `Kind` column would be a second field saying what the tracker already says, and two fields disagree the moment someone edits one and not the other - the same reason this file has no priority column. **The rule:** a bug is something already agreed not being honoured, or that cannot be - code failing a requirement, or a requirement unfollowable as written. An enhancement is anything nothing agreed covers yet. **Kind belongs to the intent, not to the backlog item**, because you cannot know which it is until it has been classified against the spec - an item in the queue has not been, and labelling it early would be deciding at the moment it was written down instead of at intake. The four open intents are labelled: #2 and #3 bug, #1 and #7 enhancement |
| B25 | The runner aggregates per-file exits by numeric maximum | `done` | — | graduate, 2026-08-30 | `run-checks.sh` takes `worst` as the largest exit code across files in `mode: per_file`. Higher-is-worse is a convention, not a fact, and each check already DECLARES which codes mean pass, fail and refused in its own `exit_codes` map. A check with inverted semantics - grep, where 0 means a match was found and therefore a violation - reports PASS on a violating tree, because 1 (no match, clean) outranks 0 (match, failed). Measured. The generated CI gate works around it with `mode: batch`; the assumption underneath is unfixed, and it silently inverts a verdict rather than erroring. **Closed 2026-08-31.** Reproduced first: a `per_file` check declaring `pass: [1]` and `fail: [0]` - grep, where 0 means the forbidden thing was FOUND - over a tree with one clean file and one violating file reported **PASS**, because `max(0,1)` is 1 and 1 was the passing code. The check found the violation and the stage reported clean. Aggregation now uses the check's OWN declared map, ranked refused > fail > pass, and the first file reaching the worst outcome donates its exit code so whatever maps it downstream sees a code the check itself declared. An UNDECLARED code outranks everything and lands as `unmapped_exit`: a check that returned something its own map does not cover has not passed, because nobody knows what it did. Proven three ways - violating tree now FAIL/rc=3, clean tree still PASS/rc=0, undeclared code REFUSED. Real suite unaffected: 15 of 15 |
| B26 | The runner validates argv[0] and never the arguments | `done` | [#2](https://github.com/gitayg/productizer/issues/2) | security review, 2026-08-30 | `run-checks.sh` `argv_of()` blocks a shell or an interpreter carrying an inline `-c`/`-e` flag, but inspects only `value[0]`. Three bypasses were reproduced, all exiting 0 with a payload executed: a program taken positionally rather than by flag; a repo-local script passed as an ARGUMENT, which walks past the repo-local guard that defaults to closed specifically so a cloned repo cannot choose what runs on the machine that cloned it; and a build tool named by neither list. `run-runner.sh` already validates every element of argv and refuses all three, so the correct behaviour exists in this repo and the gate does not use it. This is the plugin's central security claim. **Stage 1, 2026-08-30 - classified `extend`.** Intent [#2](https://github.com/gitayg/productizer/issues/2), labelled `sdlc:intent`. Not `contradict`: R18 stays true and stays asserted, it is simply narrower than P4, the principle it is listed as enforcing. Not `duplicate`: nothing active covers argv positions after the first. Not `refine`: R18's text is accurate for what it says. **This is also the B1 vehicle** - the first intent to go through all nine stages, through a PR. For intake: R18's acceptance row names the implementation (`argv[0] validation`) rather than the obligation, so it can never fail; the new id needs a row that names the obligation **Merged 2026-09-01 as R29**, claimed Covered by `untrusted-execution`: the three bypasses are each constructed and each refused. **Closed 2026-09-01, merged as R29**, claimed Covered by `untrusted-execution`: the three bypasses are each constructed and each refused. |
| B27 | Build the ten verifiers the interrogation specified | `done` | — | interrogation, 2026-08-30 | R3 diffs superseded text against git history, which makes history a check input for the first time. R4 asserts both halves of read-only - the generator writing nothing AND the page declaring no capability to republish itself. R6 proves every active id reached the classifier. R8 asserts every added requirement has a row, advisory first because it starts red. R9 generates a requirements section of the guide. R10 fails an unmarked requirement after an import. R12 blocks only a spec change touching the requirement a pending ruling was raised against. R13 commits the absent-tool fixture as a standing test. R19 records the spec commit and hash with every classification, sharing R6's mechanism. R7 needs a refine intent before it can be satisfied at all. **Closed 2026-08-30.** Nine verifiers built and declared, taking the suite from 4 checks to 15, all passing. R3 reads git history - the first check here to do so - choosing a baseline PER REQUIREMENT, since one ref is not the pre-supersede commit for every id. R12 blocks only the requirement a pending ruling names and PRINTS the allocations it lets through, so the decision not to block is visible. R6 and R19 share one mechanism: a classification records the spec commit and hash it was made against, so classifying from a remembered copy became impossible to RECORD rather than merely forbidden. R13's fixture guards its own premise - point it at an installed tool and it reports unmeasured, not a pass. R7 alone is unbuildable as written and is now intent #3 |
| B28 | Spec coverage cannot be turned on without refusing every run | `done` | — | measurement, 2026-08-30 | `policy.spec_coverage` takes `require`, `auto` or `off` - there is no threshold. It defaults to `auto`, meaning measure as soon as ANY check declares `coverage.spec_units`, so declaring it on one check flips the whole repo: **measured, 24 of 25 requirements Missing, run refused.** Even with all fifteen checks mapped, roughly half the active requirements still have no verifier - R2, R5, R7, R11, R15, R17, R18, R20, R22, R24, R25, R26, R27, R28 - so the count would refuse. Declaring the mapping alongside `spec_coverage: off` does pass, and was rejected: a mapping nothing validates drifts, and it would trade an accurate `UNMEASURED (not_declared)` for a weaker `committed opt-out`. The honest options are to finish the verifiers first, or to add a threshold mode to the runner. Note `Missing` is deliberately not claimable - it is what the runner concludes about a unit nothing covered, which is why a check cannot declare itself honest. **2026-08-31 — three of four blockers closed, and the mode problem is worse than this item said.** R24 has `nothing-merged`, R26 has `no-fabricated-zero`, and R27/R28 are measured `n/a` by `jira-unbound` — the guard is shut and re-measured every run, so the n/a expires by itself. Coverage went 25 uncovered → 21. But declaring those four claims TURNED ENFORCEMENT ON and made CI refuse every run: `auto` is all-or-nothing, `SPEC_ENFORCED = require or (auto and _claim_count > 0)`, so the first honest claim anybody records refuses on everything nobody has mapped yet. Found by CI going red, not by reading the code. Fixed by adding a `report` mode — measures and never refuses, `declared but not enforced` — and this repo now sits on it. **Remaining: map the other 21 requirements, then flip to `require` as the last step**. **2026-09-01 — mapped. 21 uncovered → 3.** Ten existing checks were AUDITED before any claim was declared, and **not one of the ten was `Covered`**: eight Partial, two asserting nothing at all. Declaring on name-match would have recorded ten claims, eight overstated and two false. The two that assert nothing: `classification-provenance` sweeps an empty set because intake never calls `record-classification.sh`, and `spec-home` was paired with R19 by mistake - its own header says it asserts R1, and its output is byte-identical whether R19 is obeyed or violated. Four new checks built and each observed FAILING before being declared. R17 split into two declarations because only its blocking half exists (B30). **Remaining Missing: R6, R7, R19** - R7 is contested and awaiting its refinement to R32. `require` still not flipped: 9 Partial verdicts are real gaps, each named in the run output, and flipping while they stand would refuse every run **Closed 2026-09-02 by redefining the setting, because as written it was unreachable.** Coverage reached 0 Missing, but `require` also refused on `Partial`, and five obligations here cannot be fully proven from inside a repository at all: R9 needs reading rather than matching and `models.checks` forbids a model in this path; R30's device half is a fact about the publishing service; R31's refusal path needs a forbidden publish attempted; R6 covers classifications that merge nothing and so write nothing; R19 names a situation this repository has never been in. Demanding `Covered` for those demanded a proof that does not exist, and a gate that cannot be satisfied is one somebody switches off. **The bar is now: nothing may be silently unproven.** `Missing` refuses. A `Partial` is accepted only because the loader now REFUSES a Partial with no `reason`, so every one has already stated which part is unasserted. Falsified both ways - a requirement no check names exits 3, a Partial without a reason is a config load error - and `policy.spec_coverage` is `require` |
| B29 | A timed-out check records `covered: 0` | `done` | — | R26 verifier, 2026-08-31 | `run-checks.sh` initialises `covered = 0` and still builds a coverage block for a check whose status is `timeout`, which is in `CANNOT_RUN`. A `stdout_count` check killed at its limit therefore records a zero for a number nobody measured - exactly the P1 failure, in the runner that enforces P1. The `no-fabricated-zero` check's seventh assertion would flag it, but **no timeout fixture exists, so this is reasoning and not a measurement**, and it is written down as such. Reproduce it first: a fixture whose command sleeps past its `timeout_seconds`, then read the recorded `coverage` block. **Closed 2026-09-01. Real, and wider than this item said.** Reproduced on a fixture whose command sleeps past its limit: the killed check printed nothing and the runner recorded `covered: 0`. Not just `timeout` - **four of the five cannot-run statuses fabricated** (`timeout`, `no_version`, `refused`, `unmapped_exit`); `missing_tool` was the only clean one, because it returns before the coverage block is built. Fixed by deleting the coverage key on any row that reached no verdict - absent, matching what `missing_tool` already did, so no third convention. **The mirror-image bug was checked explicitly**: a check that RAN and covered nothing still records its real `0`. The reason this survived is worth keeping: `no-fabricated-zero`'s seventh assertion sweeps exactly this and had been sweeping an EMPTY SET since it was written, because its fixture only ever produced the one clean status. New check `cannot-run-coverage` reaches the state by a different mechanism and its premise guard refuses if pointed back at the old fixture |
| B30 | The publish gate has no approval path, and argues against the only one it leaves | `done` | — | measurement, 2026-09-01 | R17 says the gate shall block a publishing command **until a person decides**. `.claude/hooks/publish-gate.sh` implements the blocking and implements no deciding: there is no env var, no marker file, no signed-approval mechanism, no override of any kind - grepped for `APPROV|approve|allow|env|getenv|marker|override`, and the only hits are comments. So a person who has decided **yes** cannot express it, and the sole remaining route is retyping the command in their own shell. The gate's own closing paragraph argues against exactly that: *"They should not have to retype a command they did not compose; making them do that is how the approval turns into a chore and the chore turns into a rubber stamp."* It describes the failure it causes. **Measured, not argued: three pushes were blocked this way in one session after the checklist was shown and an explicit yes was given.** This also means R17 is only half implemented - the block is real, the *until a person decides* is not - so a verifier written against R17's full obligation should FAIL today rather than be narrowed to fit. Design question intake must settle: what counts as a person deciding, given the decision must not be forgeable by the agent the gate exists to constrain, and must not become a rubber stamp. **Closed 2026-09-01.** The gate now returns `permissionDecision: "ask"`, which routes to Claude Code's own permission prompt - drawn by the harness, answered by a keystroke, and unreachable from inside the process being gated. The person approves the command already written instead of retyping it, which is what the gate's own closing paragraph asked for. **It fails closed on what the docs do not promise:** the documentation says a `deny` survives `bypassPermissions` and `--dangerously-skip-permissions` and says NOTHING of the same for `ask`, so any mode not documented to prompt still gets the hard refusal. A recognised publish asks; one the gate could not parse still refuses, because approving something nobody can read is a signature on a blank page. An `ask` also requires a fresh checklist naming that exact command - not a security boundary, since the agent writes it, but it makes the prompt show what was checked rather than a bare "allow?" |
| B31 | Two checks covering complementary halves of one requirement can never read `Covered` | `done` | — | measurement, 2026-09-01 | The runner takes the BEST SINGLE claim per requirement, not a union: `uv = Covered if any claim is Covered, else Partial if any is Partial`. So a requirement whose two obligations are asserted by two different checks reads `Partial` forever, even though both halves are genuinely covered. Live examples: **R23** - the stop half by `solver-corpus`, the ask half by `ruling-requested`; **R8** - the allocation half by `spec-integrity`, the acceptance-row half by `acceptance-rows`. Two candidate fixes and they are not equivalent. (a) Teach the runner to union partial claims - but a union of two vague Partials would read as full coverage, which is the hollow green this stage exists to prevent, so it would need each claim to name WHICH half it covers and the runner to check the halves are disjoint and exhaustive. (b) **Split the requirement**, which is what this repo already did to R14, R16 and R21 for carrying two obligations under one id - and both R23 and R8 do exactly that (`stop AND ask`, `allocate AND record`). (b) is consistent with the existing rule and needs no runner change; it is a spec change and therefore goes through intake **Closed 2026-09-01.** R23 -> R33 (stop) + R34 (ask); R8 -> R35 (allocate) + R36 (record). R33 and R35 are Covered outright. The split broke a citation chain - R14 pointed at R23, now superseded - and `superseded-text` caught it. |
| B32 | A generated file is committed, so it conflicts on every merge | `done` | — | measurement, 2026-09-01 | `.claude/productizer/checks-result.json` is written by `run-checks.sh` and committed, so any two branches that both ran the suite conflict there - and the conflict is meaningless, because the only correct resolution is to regenerate rather than to choose a side. **Measured, not argued:** rebasing PR #4 onto main conflicts in exactly two files, and this is one of them; it has also conflicted on every cherry-pick and merge attempted in this repository. Worse, a hand-resolved result file is a RECORDED MEASUREMENT NOBODY TOOK - someone picks a hunk and the file then reports a run that never happened, which is the P1 failure with a merge tool holding the pen. Options: stop committing it and publish it as a CI artifact instead (loses the offline dashboard, which reads it from the repo); keep it committed but add a merge driver that regenerates rather than merges; or split the human-meaningful summary from the volatile detail. The dashboard's dependency on it is the constraint that decides this **Closed 2026-09-01.** `checks-result.json` is untracked and ignored; every reader was checked and already degrades honestly without it. A merge driver and `.gitattributes` remain as defence-in-depth, inert until two `git config` lines are run. **Reopened and actually closed 2026-09-02.** The closure above was false and is left standing rather than edited, because a backlog that quietly rewrites its own history is worth less than one that records a wrong entry. `.gitignore` carried the path, but the file was never removed from the index - and a tracked file ignores `.gitignore` entirely, so every claim of "untracked and ignored" had been describing an intention. Measured: `git cat-file -e HEAD:.claude/productizer/checks-result.json` succeeded and `git check-ignore` reported no match, which is what a tracked-and-listed file looks like. `git rm --cached` was run; `check-ignore` now names line 35. Two rows of `references/compliance-map.md` had meanwhile cited the committed file as evidence for SSDF PO.4.2 and AI RMF GOVERN 1.4 - PO.4.2 drops from DOCUMENTED to PARTIAL, because gathering survives and safeguarding does not. **And it turned CI red, which is the part worth keeping.** Untracking the file put a DELETED path in the next change, and `must_cover: all_triggering` demanded coverage for a file no tool can open: `covered 7/8`, hollow, blocked. It passed locally because `git rm --cached` leaves the file on disk, so this machine could still read it while a fresh checkout could not - the plainest works-on-my-machine this repository has produced. Fixed in the runner rather than the check: a path absent from the tree is dropped from the requirement and COUNTED on the rendered line, because a rule that quietly shrinks what it demands is the same failure as a check that quietly shrinks what it examined. Falsified on a two-commit fixture mixing one deletion with one modification - with the fix `PASS covered 1/2 files (1 deleted by this change, not required)`, with the fix reverted and nothing else changed `HOLLOW covered 1/2 files`. NOT verified: that a file which EXISTS and goes unexamined still reports hollow. It is unchanged by construction - the new branch is only reached when the path is absent - but two attempts to exercise it end to end hit unrelated guards, a directory being refused at argument validation and a NUL-holding file tripping the control-character check, so it was written down as unproven rather than assumed. **Now proven, 2026-09-02.** The two failed attempts were my own misuse: `--changed` takes a FILE LISTING paths, one per line, not a path - so `--changed probe-bin.dat` fed the binary's CONTENTS in as the changed list and its NUL tripped the control-character guard, and a directory is refused because the argument check tests for a regular file. With a correct list naming an examinable file and an existing binary one, `hygiene` reports `HOLLOW covered 1/2 files` and blocks. The gate is not weakened: only a path absent from the tree is excused. |
| B33 | The publish gate exists twice and nothing keeps the copies in sync | `done` | — | measurement, 2026-09-01 | `.claude/hooks/publish-gate.sh` (this repo's installed hook) and `plugins/productizer/skills/spec/templates/publish-gate.sh` (what the plugin ships) were byte-identical, and no check asserted it. **Measured:** editing one of them drifted them immediately, and the drift was invisible until `publish-gate-decides` - which drives the TEMPLATE - failed against a fix that had only landed in the hook. The failure mode is the dangerous direction: this repo's own gate can be hardened while every repo installing the plugin keeps the weaker one, and the suite stays green because it only ever tests the template. A `cmp` of the two is a three-line check. **Closed 2026-09-01.** Declared as the `installed-copies` check: every `*.sh` under `templates/` with a counterpart in `.claude/hooks/` must be byte-identical to it, and the counterpart must be executable - a hook the shell will not run is a gate that is absent, quietly. **Seeds are excluded deliberately**: spec.md, backlog.md, config.json, constitution.md and checks.yaml are copy-then-edit files that differ the moment the repo is used, and comparing them would go red on five files for doing their job. The empty set is guarded - no pair to compare is exit 2, not a pass. Falsified four ways: an appended copy, a mid-file edit, a non-executable copy, and no pair at all. Building it surfaced a fourth `set -e` trap of the session: `cmp` exits 1 when files differ, `pipefail` promotes that to the pipeline's status, and the script died mid-branch having printed the right exit code and no reason at all |
| B34 | `record-classification.sh --store` with an absolute path writes inside the repository | `done` | — | measurement, 2026-09-01 | Invoked with `--store /private/tmp/.../rec`, the recorder printed that absolute path as where it wrote - and wrote to `<repo>/private/tmp/.../rec/999.md` instead, creating a `private/` tree inside the work tree. **Found by accident:** a later run reported `REFUSED: this intent is already classified` while the named store was empty, because the guard was reading the repo-relative copy. The message named one path and the write went to another, which is worse than either failing or succeeding. Two defects in one: `--store` is joined to the root without checking whether it is absolute, and the printed path is not the path used. A tool that writes into the repository it is recording ABOUT also brushes against R4's spirit. The stray file was removed by hand; nothing asserts it cannot come back **Closed 2026-09-01.** An absolute `--store` is honoured as absolute, and the printed path is the path actually written, shortened to repo-relative when inside the repo. |

## Items tracked in Jira

An item may name a Jira key. When it does:

- **Jira owns the status.** The `Status` column shows what Jira last said, and
  the local status vocabulary above does not apply. Two systems tracking one
  item's state disagree within a week, and the one people update is the one
  people look at.
- **The mapping is declared once**, in `.claude/productizer/config.json` under
  `jira.status_map`, so "In Progress" and "In Dev" resolve to the same thing
  without anyone guessing per item.
- **Nothing is written back to Jira from here.** Reading is safe; writing puts
  this file in an argument with a workflow, a board and an automation rule that
  it will lose. Moving a Jira ticket happens in Jira.
- **The key is the join.** Never re-title an item to match its ticket — titles
  drift, keys do not.
- **If Jira cannot be read**, say so on the row rather than showing a stale
  status as if it were current. `status: unknown (Jira unreachable <when>)`.

## Starting work

Moving an item to `in-progress` is Stage 1: it becomes an intent, gets an issue,
and enters intake. Record the issue number in Notes so the item and the intent
point at each other.

**Starting work does not agree anything.** An item can be picked up, taken
through intake, and refused because it contradicts a requirement someone agreed
to two years ago. That is intake working, not a mistake — and it is the reason
the backlog is not the spec.

## What does not belong here

- **Requirements.** If it is agreed, it belongs in the spec with an `R` id.
- **Bugs against agreed behaviour.** Those are drift or incidents: they enter at
  Stage 1 as their own intent and get fixed against a requirement that already
  exists.
- **Anything already in the spec.** Intake will classify it a duplicate, which
  is the correct answer arrived at expensively.
